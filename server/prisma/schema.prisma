generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// ─── CATEGORY ─────────────────────────────────────────────────────────────────

model Category {
  id        String    @id @default(uuid())
  name      String    @unique
  products  Product[]
  createdAt DateTime  @default(now())
}

// ─── PRODUCT ──────────────────────────────────────────────────────────────────

model Product {
  id          String   @id @default(uuid())
  categoryId  String
  name        String
  brand       String
  description String?  @db.Text
  imageUrl    String?
  price       Float    @default(0)
  costPrice   Float?
  salePrice   Float?
  stock       Int      @default(0)
  isPublished Boolean  @default(false)

  cpuSpec         CpuSpec?
  gpuSpec         GpuSpec?
  ramSpec         RamSpec?
  motherboardSpec MotherboardSpec?
  psuSpec         PsuSpec?
  caseSpec        CaseSpec?
  coolerSpec      CoolerSpec?
  monitorSpec     MonitorSpec?
  storageSpec     StorageSpec?
  laptopSpec      LaptopSpec?
  pcBuildSpec     PcBuildSpec?
  furnitureSpec   FurnitureSpec?

  category   Category    @relation(fields: [categoryId], references: [id])
  orderItems OrderItem[]
  cartItems  CartItem[]
  wishlists  Wishlist[]

  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([categoryId, isPublished])
}

// ─── SPEC TABLES ──────────────────────────────────────────────────────────────

model CpuSpec {
  id            String  @id @default(uuid())
  productId     String  @unique
  product       Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  socket        String
  cores         Int
  threads       Int
  baseClockGhz  Float
  boostClockGhz Float
  tdp           Int
  cacheL3       String?
  generation    String?
}

model GpuSpec {
  id            String  @id @default(uuid())
  productId     String  @unique
  product       Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  vramGb        Int
  tdp           Int
  lengthMm      Int?
  pcieGen       Int?
  boostClockMhz Int?
  memType       String?
}

model RamSpec {
  id         String  @id @default(uuid())
  productId  String  @unique
  product    Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  capacityGb Int
  speedMhz   Int
  generation String
  latency    String?
  kit        String?
}

model MotherboardSpec {
  id         String  @id @default(uuid())
  productId  String  @unique
  product    Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  socket     String
  chipset    String?
  formFactor String
  ramGen     String
  ramSlots   Int
  maxRamGb   Int?
}

model PsuSpec {
  id         String  @id @default(uuid())
  productId  String  @unique
  product    Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  wattage    Int
  efficiency String?
  modular    String?
}

model CaseSpec {
  id              String  @id @default(uuid())
  productId       String  @unique
  product         Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  formFactor      String
  maxGpuLengthMm  Int?
  radiatorSupport String?
  driveBays       Int?
}

model CoolerSpec {
  id             String  @id @default(uuid())
  productId      String  @unique
  product        Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  coolerType     String
  tdpRating      Int?
  radiatorSizeMm Int?
  socketSupport  String?
}

model MonitorSpec {
  id            String  @id @default(uuid())
  productId     String  @unique
  product       Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  sizeIn        Float
  resolution    String
  refreshRateHz Int
  panelType     String?
  responseMs    Float?
  hdr           Boolean @default(false)
}

model StorageSpec {
  id            String  @id @default(uuid())
  productId     String  @unique
  product       Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  capacityGb    Int
  storageType   String
  interfaceType String?
  readMbps      Int?
  writeMbps     Int?
}

model LaptopSpec {
  id                String  @id @default(uuid())
  productId         String  @unique
  product           Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  cpu               String
  gpu               String?
  ramGb             Int
  storageGb         Int
  displaySizeIn     Float
  displayResolution String?
  os                String?
}

enum PcBuildType {
  GAMING_ESPORT
  WORKSTATION
  MINI_SFF
}

model PcBuildSpec {
  id        String      @id @default(uuid())
  productId String      @unique
  product   Product     @relation(fields: [productId], references: [id], onDelete: Cascade)
  buildType PcBuildType
}

enum FurnitureType {
  CHAIR
  DESK
}

model FurnitureSpec {
  id            String        @id @default(uuid())
  productId     String        @unique
  product       Product       @relation(fields: [productId], references: [id], onDelete: Cascade)
  furnitureType FurnitureType
}

// ─── USER ─────────────────────────────────────────────────────────────────────

enum Role {
  USER
  STAFF
  ADMIN
}

model User {
  id                 String    @id @default(uuid())
  email              String    @unique
  password           String
  fullName           String
  phone              String?
  avatarUrl          String?
  role               Role      @default(USER)
  googleId           String?   @unique
  resetToken         String?   @unique
  resetTokenExpiry   DateTime?
  isEmailVerified    Boolean   @default(false)
  verifyToken        String?   @unique
  verifyTokenExpiry  DateTime?

  orders    Order[]
  cart      Cart?
  wishlists Wishlist[]
  addresses Address[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// ─── ADDRESS BOOK ─────────────────────────────────────────────────────────────

model Address {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  label     String?
  recipient String
  phone     String
  street    String
  ward      String
  city      String
  isDefault Boolean  @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
}

// ─── CART ─────────────────────────────────────────────────────────────────────

model Cart {
  id     String     @id @default(uuid())
  userId String     @unique
  user   User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  items  CartItem[]
}

model CartItem {
  id            String  @id @default(uuid())
  cartId        String
  productId     String
  quantity      Int     @default(1)
  customBuildId String?

  cart    Cart    @relation(fields: [cartId], references: [id], onDelete: Cascade)
  product Product @relation(fields: [productId], references: [id])

  @@unique([cartId, productId])
  @@index([cartId])
}

// ─── ORDER ────────────────────────────────────────────────────────────────────

enum OrderStatus {
  PENDING                 // MoMo only — awaiting payment
  AWAITING_CONFIRMATION   // Paid (or COD) — staff must Accept or Reject before fulfillment
  PROCESSING              // Accepted — being prepared
  SHIPPED
  DELIVERED
  CANCELLED
}

model Order {
  id            String      @id @default(uuid())
  userId        String?
  guestEmail    String?
  subTotal      Float       @default(0)
  discount      Float       @default(0)
  shippingFee   Float       @default(30000)
  totalAmount   Float
  couponCode    String?
  status        OrderStatus @default(PENDING)
  paymentMethod String
  isPaid        Boolean     @default(false)
  shippingInfo  Json
  momoOrderId   String?     @unique
  momoTransId   String?
  refundedAt    DateTime?

  user  User?       @relation(fields: [userId], references: [id])
  items OrderItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model OrderItem {
  id         String  @id @default(uuid())
  orderId    String
  productId  String
  quantity   Int
  priceAtBuy Float

  order   Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product Product @relation(fields: [productId], references: [id])
}

// ─── WISHLIST ─────────────────────────────────────────────────────────────────

model Wishlist {
  id        String   @id @default(uuid())
  userId    String
  productId String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, productId])
}

// ─── COUPON ───────────────────────────────────────────────────────────────────

model Coupon {
  id            String    @id @default(uuid())
  code          String    @unique
  discountPct   Float?
  discountFixed Float?
  minOrderValue Float     @default(0)
  maxUse        Int       @default(1000)
  usedCount     Int       @default(0)
  isActive      Boolean   @default(true)
  validUntil    DateTime?
  createdAt     DateTime  @default(now())
}

// ─── SITE SETTINGS ────────────────────────────────────────────────────────────

model SiteSetting {
  key       String   @id   // e.g. "hero_video_url", "hero_poster_url"
  value     String   @db.Text
  updatedAt DateTime @updatedAt
}

// ─── NEWSLETTER ───────────────────────────────────────────────────────────────

model NewsletterSubscriber {
  id               String   @id @default(uuid())
  email            String   @unique
  unsubscribeToken String   @unique @default(uuid())
  source           String?
  isActive         Boolean  @default(true)
  subscribedAt     DateTime @default(now())
}
