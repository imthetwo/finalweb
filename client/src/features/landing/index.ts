// ── Public API (gatekeeper) for the `landing` feature ──────────────────────
// Import landing's surface from here, not from internal subfolders.
export { default as LandingPage } from "./LandingPage";
