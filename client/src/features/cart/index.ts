// ── Public API (gatekeeper) for the `cart` feature ──────────────────────────
// Import cart's surface from here, not from internal subfolders.
export { CartView } from "./components/CartView";
export { useAddToCart } from "./hooks/useAddToCart";
